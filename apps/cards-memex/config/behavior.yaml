# Behavior configuration for the Cards Strategy Bot

personality:
  tone: "Be concise and professional"
  emoji_usage: "minimal"  # Options: "minimal", "moderate", "frequent"

rate_limits:
  qa_calls_per_minute: 12
  classify_calls_per_minute: 8

